[project]
name = "amf-ari"
version = "0.1.0"
description = "AMF Argument Relation Identification"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "flask==3.1.3",
    "gunicorn==25.1.0",
    "torch==2.7.1",
    "transformers==4.57.6",
    "requests==2.32.5",
    "xaif-eval==0.2.2",
]

[tool.uv]
environments = ["sys_platform == 'linux' and platform_machine == 'x86_64'"]
